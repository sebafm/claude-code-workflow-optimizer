# 🚨 CRITICAL: Claude Code runtime data (user-specific optimization sessions)
# 
# .claude/optimize/ contains IRREPLACEABLE user session data:
# - decisions/ - User decision records and audit trails  
# - completed/ - Completed optimization tracking
# - agents/ - Dynamic agent assignments and configurations
# - backlog.json - User-specific issue tracking
#
# ⚠️  NEVER commit this directory - it contains personal optimization history
# ✅  Each user maintains their own optimization session data
# 🛡️  Use tar.gz backup system (see commit-changes.md) for data protection
#
.claude/optimize/
.claude/settings.local.json

# Keep development tools and project deliverables
# .claude/agents/ - commit (our developed agents)
# .claude/commands/ - commit (development tools like commit-changes.md)  
# commands/ - commit (our main product)

# IDE and editor files
.vscode/settings.json
.vscode/launch.json
.idea/
*.code-workspace
*.swp
*.swo
*~

# System files
.DS_Store
Thumbs.db
.Trash-*

# Logs and temporary files
*.log
*.tmp
*.temp
.cache/

# Environment files
.env
.env.local
.env.production

# Node.js (if we add any tooling later)
node_modules/
npm-debug.log*

# Archives and packages
*.zip
*.tar.gz
*.rar

# Backup files
*.bak
*.backup
